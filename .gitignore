# nflverse downloads and intermediate frames — regenerated by refresh.py on every
# run, ~160 MB, and must never reach the repo.
cache/

# Python build artifacts
__pycache__/
*.py[cod]

# local virtualenvs
.venv/
venv/

# OS cruft that Finder drops into folders before a web upload
.DS_Store
